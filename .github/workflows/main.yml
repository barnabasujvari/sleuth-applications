name: Release
on:
  release:
    types: [published]
jobs:
  push_to_registry:
    name: Test and Push Docker image to GitHub Packages
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v2
      - name: Push to GitHub Packages
        uses: docker/build-push-action@v1
        with:
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          registry: docker.pkg.github.com
          repository: sleuth-capital/${{ github.event.repository.name }}/${{ github.event.repository.name }}
          tag_with_ref: true
